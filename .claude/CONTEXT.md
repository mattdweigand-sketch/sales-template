# Generated command pointers

Inputs: scripts/wrapper-contract.json. Run scripts/wrappers.py to generate commands/.
Read the canonical AGENTS.md at the repository root before invoking a command.
Human check: run scripts/wrappers.py --check and inspect route targets.
