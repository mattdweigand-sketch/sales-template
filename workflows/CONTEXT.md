# Workflow library

The [root router](../CONTEXT.md) chooses a family; each family chooses one procedure.
The [run contract](run.md) owns the shared review/apply lifecycle.
Factory inputs live under _shared/. Working artifacts live only under output/{run-id}/.
